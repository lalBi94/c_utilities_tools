#include <stdio.h>
#include <stdlib.h>
#include "lib_promo.h"

int main(int argc, char* argv[]){
    if(argc != 3){
        exit(0);
    }

    else{
        printf("Old Price = %s$, New Price = %lf", reduc(ARGV1, ARGV2));
    }
}